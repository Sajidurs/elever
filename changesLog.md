# Elever Notes — Changes Log

> Running log of work done in this project. Updated after every task so future sessions have full context without re-reading every file diff. See [brandguide.md](brandguide.md) for brand rules.

---

## 2026-07-23

- Log file created. No feature work yet.
- Reviewed brandguide.md and current site structure with user; agreed to rebuild as a simple ecommerce site (Supabase backend). Full architecture plan written and approved: see `C:\Users\bappe\.claude\plans\woolly-tickling-hare.md` for full detail. Key decisions locked in:
  - Final public pages: Home, About, Features (general brand/benefits), Shop (dynamic product catalog + detail), Contact. Plus a public Activate page (QR destination, not in main nav) and a separate `admin.html` admin tool.
  - Reviews folds into Home/Shop as a section; FAQ and Shipping & Returns pages are dropped for now.
  - Checkout = Cash on Delivery only (Full Name, Phone, Address fields), no payment gateway, no multi-item cart (single SKU for now).
  - QR activation = one universal QR/link for every notebook (not per-serial). Activate page collects name/phone/email, shows a success popup with discount text + PDF download + Play Store link (all editable from admin Settings).
  - Product fields: title, short_description, regular_price, sale_price, plus image_url/is_active/slug as necessary scaffolding.
  - Routing: adding hash-based routing (`#/shop/g1` etc.) since the site currently has zero URL routing (pure in-memory `state.page`) and hosting host is not yet chosen.
  - `d:\Elever\schema.sql` written with full Supabase schema (products, orders, activations, settings, messages tables + RLS policies) — ready for user to run in the Supabase SQL editor.
  - Still needed before implementation continues: user to create the Supabase project, run `schema.sql`, create one admin auth user, and provide Project URL + anon key.
- User ran the schema SQL and provided Supabase Project URL + anon key (project `ksdprcoizcfisfhvpacm`). Confirmed `messages` table for Contact form (schema as-run includes it).
- Reorganized new backend files into folders for a cleaner project layout: `schema.sql` moved to `d:\Elever\supabase\schema.sql`; admin tool moved to `d:\Elever\admin\` (`index.html` + `admin.js`) — served at `/admin/` once hosted, not linked from any public nav.
- Built the admin panel (`d:\Elever\admin\index.html`, `d:\Elever\admin\admin.js`): Supabase Auth email/password login gate; tabs for Products (add/edit/delete, image upload to `product-images` storage bucket, active/hidden toggle, auto-slug from title), Orders (list + status dropdown: pending/fulfilled/cancelled), Activations (read-only list), Messages (read-only list), Settings (discount text, PDF link, Play Store link). Supabase URL/anon key are hardcoded constants at the top of `admin.js` (anon key is meant to be public; RLS is the real security boundary — no service-role key used).
- Not yet tested against the live Supabase project (schema was just run) — next step is to log in and verify each tab before wiring the public site to the same tables.
- User confirmed admin login works. Storage bucket `product-images` was missing (schema.sql only had it as a comment) — fixed by adding an active `insert into storage.buckets ...` + policies block to `supabase/schema.sql`, user ran it, upload works.
- **Rebuilt `Elever Notes.dc.html` end to end per the approved plan.** All changes live in the one file; `support.js`/`image-slot.js` untouched.
  - **Supabase wiring**: `@supabase/supabase-js` v2 loaded via CDN `<script>` in `<helmet>`; lazy `sb()` singleton guards against the async CDN-load race (throws only if called before load, never blocks initial render).
  - **Hash routing**: `go(page, params)` now writes `location.hash` (`#/shop/g1`, `#/activate`, etc.) and a `hashchange` listener keeps state in sync — Back/Forward work, cold deep-links resolve correctly (verified with Playwright: direct load of `#/about`, `#/shop/g1` etc. all render the right page with no flash of Home).
  - **Shop** (new `isShop` page): fetches `products` on mount, listing grid (image/title/short description/price with sale strikethrough) → product detail page → quantity stepper → COD checkout (Full Name, Phone, Address) → `orders` insert, snapshotting `product_title`/`product_price` at time of order. Verified end-to-end against a real seeded product (G1) — order appeared correctly in admin Orders tab. Now also verified working with 2 products in the catalog (G1 + a second "G2" product the user added independently), confirming the catalog isn't hardcoded to one SKU.
  - **Features** (new `isFeatures` page): general brand/methodology page — the 4 core "how Elever thinks" benefit cards, a new **Research** section citing Matthews/Gollwitzer & Sheeran/Gloria Mark/Lally et al with honest disclaimers (per brandguide's citation rules — this content didn't exist anywhere in the old site), and a generalized **activation ecosystem** explainer (QR → discount/app/PDF) linking to Activate.
  - **Reviews** folded into Home (existing teaser, untouched) and a new full ratings/sort/masonry-grid section on the Shop listing page (`id="shop-reviews"`), reusing the original `ratingBreakdown`/`allReviews`/`sortOptions` data and logic. Standalone Reviews page deleted; `goReviews` now routes to `#/shop` and scrolls to that section.
  - **Activate page** repurposed: dropped the old per-notebook "activation code" field (universal QR needs none), added a Full Name field, wired a real `submitActivation()` → inserts into `activations`, fetches `settings`, and shows a success popup with the customer's name, the configured discount text, and conditional Download-PDF / Get-the-App buttons (only shown if those settings links are filled in). Verified: empty-submit validation, full submit → popup, correct discount text pulled from Settings.
  - **Contact page** repurposed: real controlled form (first/last name, email, subject, message) → `submitMessage()` → inserts into `messages`, inline "Message sent" confirmation on success. Verified end-to-end.
  - **Nav/footer/dead-link cleanup**: header nav rewritten to the final 5 items (Home, About, Features, Shop, Contact); footer rewritten to 3 columns (Shop, Company, Support) pointing only at surviving pages; Contact's bottom link cards updated (dropped FAQ/Shipping links, added Shop).
  - **Deleted entirely**: the ad-traffic Landing Page (`isLp` + video lightbox), the old single-SKU Product page, the standalone FAQ page, and the standalone Shipping & Returns page — along with all now-dead state (`openFaq`, `openQ`, `lpDay`, `lpVid`), methods (`_startCd`/`_countUp` countdown+count-up helpers, `setDay`/`openVid`/`closeVid`/`goProductSection`/`toggleFaq`/`toggleQ`), and data arrays (`faqData`, `shippingOptions`, `policyBlocks`, `returnSteps`, `trustBadges`, `counters`, `stats`, `bridge`, `valueStack`, `videos`, `chats`, `compare`, `lpFaqs`, `faqGroups`, old per-SKU `feats`/`gallery`/`specs`/`rewards`/`pdfPoints`). None of this is reachable via nav/footer/hash anymore.
  - **Home's flagship showcase** no longer hardcodes "$89" — it now reads price/title live from the first active product in the catalog (via a `featuredProduct` computed value), so it stays correct as products are edited or added in admin.
  - **SEO pass**: every page now sets a real `document.title` and a single, correctly-updating `<meta name="description">` per route (including per-product on Shop detail, pulling from the product's own short description). **Found and fixed a real bug during this pass**: templating `<title>{{ pageTitle }}</title>` directly in `<helmet>` does NOT work in this runtime — `<helmet>` content bypasses the reactive template engine entirely, so it would have shipped the literal text "{{ pageTitle }}" as the page title to every visitor. Fixed by setting `document.title` imperatively from `componentDidMount`/`componentDidUpdate` instead (same pattern already used for the meta description, which does need to be imperative since Chrome's templating would otherwise duplicate the tag).
  - Full final smoke test: all 8 routes (`/`, `/home`, `/about`, `/features`, `/shop`, `/shop/g1`, `/activate`, `/contact`) cold-load with zero console/page errors; nav click-through works; no `service_role` key anywhere in the codebase; anon key correctly blocked from reading `orders`/`activations`/`messages` (RLS confirmed working — returns empty set, not an error).
- **Known follow-ups, not yet done**: `sort_order` isn't exposed in the admin product form (defaults to 0 for everyone); no image gallery (single image per product only); no color/size variants (explicitly deferred per user); hash-based routing means individual Shop/product pages won't get separate Google-indexed listings until/unless migrated to pushState with server rewrite rules on a chosen host.
- **User requested a full rewrite: React (Next.js) + Node.js, TypeScript, professionally organized `src/` structure** — the dc-runtime site above worked but wasn't a codebase a developer could easily read (one file, no components, no types). Full plan approved: see `C:\Users\bappe\.claude\plans\woolly-tickling-hare.md`. Decisions: Next.js App Router (server-rendering fixes the hash-routing SEO gap noted above), keep Supabase exactly as-is (same schema/RLS/storage, no migration), TypeScript, admin merged into the same app under a protected `/admin` route.
  - **Safety net first**: initialized git (`git init`), committed the working dc-runtime site as a baseline, pushed to `https://github.com/Sajidurs/elever.git`. Moved the entire old site (`Elever Notes.dc.html`, `support.js`, `image-slot.js`, `admin/`, `assets/`, `uploads/`, logo PNGs) into `legacy/` — kept for reference, not deleted. `supabase/schema.sql`, `brandguide.md`, `changesLog.md` stayed at project root.
  - **Scaffolded Next.js 16 (App Router) + TypeScript + Tailwind CSS v4** at the project root via `create-next-app`. Installed `@supabase/ssr`, `@supabase/supabase-js`, `zod`.
  - **Important correction found while building**: this project scaffolded onto **Next.js 16**, which renamed `middleware.ts` → `proxy.ts` (`export function proxy(request)` instead of `middleware`) and changed Proxy to run on the **Node.js runtime by default** (previously Edge-only) — confirmed directly from the Next.js docs bundled in `node_modules/next/dist/docs/` before writing any auth code, per that package's own `AGENTS.md` warning that this version has breaking changes from what most training data assumes. Also confirmed the classic `export const dynamic = 'force-dynamic'` caching model still exists as the default (the newer opt-in "Cache Components" model, gated behind `cacheComponents: true` in `next.config.ts`, was deliberately left off — unnecessary complexity at this scale).
  - **Architecture built**: `src/lib/supabase/{client,server,proxy}.ts` (browser client, Server Component/Action client via `@supabase/ssr` cookies, and the proxy session-refresh helper); `src/proxy.ts` gates `/admin/*` (redirects to `/admin/login` if no session — uses `getUser()`, not `getSession()`, since only `getUser()` revalidates against Supabase Auth); `src/lib/types/database.ts` (hand-written types mirroring the schema); `src/lib/data/*.ts` (one query-function module per table, reused by both pages and admin actions); `src/lib/validation/schemas.ts` (zod schemas per form); `src/lib/content.ts` (static marketing copy — benefits, research citations, ecosystem, reviews — separated from presentation).
  - **Public pages rebuilt as Server Components**: Home (`/`), About, Features, Shop listing + `/shop/[slug]` detail (with `generateMetadata` building real per-product `<title>`/description), Activate, Contact. All DB-backed pages (`/`, `/shop`, `/shop/[slug]`, all `/admin/*`) marked `export const dynamic = 'force-dynamic'` so admin edits show up with zero staleness — confirmed via production build output showing the correct ○ (static) vs ƒ (dynamic) split.
  - **Mutations as Server Actions** (not hand-rolled API routes), each using `useActionState` client-side for loading/error/success states: `placeOrder` (Shop checkout — re-derives price/title server-side from the product row rather than trusting hidden form fields, so a tampered client can't change what gets billed), `submitActivation` (+ `RewardsPopup` overlay pulling discount/PDF/app links from `settings`), `submitMessage`, plus admin `saveProduct`/`deleteProduct` (image upload straight to the same `product-images` bucket via `FormData`), `updateOrderStatus`, `updateSettingsAction`, and `login`/`logout`.
  - **Admin merged in** at `/admin`, gated by `src/proxy.ts` + a server-side `getUser()` check in `app/admin/(protected)/layout.tsx` as defense-in-depth, with `AdminNav` (Products/Orders/Activations/Messages/Settings) matching the same 5 sections as the old vanilla-JS tool.
  - **Verified end-to-end**: every public flow re-tested against the new app (Home showing live featured-product price, Shop listing/detail/checkout, Activate + rewards popup, Contact) — all pass with zero console errors. Admin auth guard confirmed (all `/admin/*` routes 307-redirect to `/admin/login` when logged out). User personally logged into the new `/admin` and confirmed editing a product saves correctly. `npm run build` succeeds cleanly; `npm run start` (production mode) re-verified all routes serve 200 and `curl` (no JS execution) shows the real product title/description in the raw HTML response — the actual proof the SEO migration goal was met. No `service_role` key anywhere in the new codebase either.
  - **Not yet done**: `legacy/` still exists (intentionally, per plan — safe to delete once confidence builds); no automated test suite; product `sort_order` still not exposed in the admin form (same gap as before); a real production domain/hosting target hasn't been chosen yet, so `NEXT_PUBLIC_SITE_URL` in `sitemap.ts`/`robots.ts` defaults to a placeholder (`https://elevernotes.com`) until deployment.
- **Redesigned the Home hero** (`src/components/home/Hero.tsx`) per a Web3-landing-page reference the user liked — kept the structure (floating icon badges, gradient two-tone headline, bullet checklist, dual CTA, ambient glow) but restyled entirely in Elever's dark/bronze palette instead of neon, since the brand voice explicitly rejects hype. New copy: "Organize your life, / one day at a time." Added a `.animate-float` keyframe to `globals.css` (respects `prefers-reduced-motion`).
- **Added an order tracking flow and phone-verified activation**:
  - New `supabase/002_order_tracking_and_activation_verification.sql`. Adds two `SECURITY DEFINER` Postgres functions so the anon key can support these lookups without ever exposing a raw `SELECT` on `orders` (which would let anyone dump the whole table via the public REST API): `phone_has_order(phone)` → boolean, and `track_order(phone, order_ref)` → single row (order number/product/qty/status/date only). Phone comparison strips non-digits on both sides so formatting differences don't cause false negatives.
  - New `/track-order` page + `trackOrder` Server Action: customer enters phone + order number to see status. Added "Track your order" to the footer Support column.
  - `submitActivation` now calls `phone_has_order` before inserting — if the phone doesn't match any order, returns "This phone number isn't registered to any order..." instead of silently accepting anyone.
  - Removed the redundant "Scan the QR code inside the front cover..." instruction paragraph from the Activate page form card (nonsensical once you're already on the page — the physical QR's job is just to get you there).
  - User ran the SQL and confirmed both features work end-to-end.
- **Added a dedicated order-confirmation ("thank you") page**, per user request: `/order-confirmation/[id]` shown right after checkout instead of an inline success card. Shows full order details (order number, product, quantity, total, delivery address, phone, status, date), a prominent "activate when it arrives" CTA reminding the customer to use the *same phone number*, and the full `ecosystem` benefits grid (discount/app/PDF) so the value is spelled out immediately after purchase.
  - New `supabase/003_order_confirmation.sql` — a third `SECURITY DEFINER` RPC, `get_order_confirmation(order_id)`, returning one full order row by id. Safe on id alone (no separate phone check needed) since the order UUID itself is already an unguessable secret — standard "order ID in the confirmation URL" pattern.
  - `placeOrder` now calls `redirect()` to the confirmation page instead of returning an inline success state; `OrderForm.tsx`'s old inline "Order placed" card was removed accordingly.
  - **Caught and fixed a real bug while wiring the redirect**: `placeOrder` had been doing `.insert({...}).select('id').single()` to read back the new row's id — but anon has no SELECT policy on `orders` (intentional, so the table can't be dumped via the public REST API), so PostgREST's insert-then-select-back silently failed RLS and every order placement was returning "Something went wrong" — this had been silently broken since the order-tracking feature added `.select()` and was never re-verified end-to-end after that edit. Fixed by generating the order's UUID with `crypto.randomUUID()` in the Server Action itself and passing it explicitly into the insert, so no read-back is needed at all. Re-tested end-to-end with Playwright — order now inserts and redirects correctly to `/order-confirmation/{id}`.
  - User ran the SQL and confirmed the confirmation page works end-to-end.

## 2026-07-24

- **Added admin dashboard stats, failed-activation tracking, and a premium product gallery + sticky buy bar** — three features in one session:
  - **Admin dashboard** (`/admin`, previously just a redirect to Products): real stat tiles — Orders (pending/fulfilled/cancelled/total), Activations, Failed activation attempts, Messages, all filterable by time range (Today/7 days/30 days/All time via `?range=` query param, no client JS needed, just links); Products (active/total, not time-filtered since it's a point-in-time catalog count, not an event log). New `src/lib/data/dashboard.ts` with parallel count-only Supabase queries (`{count:'exact', head:true}` — no row data fetched, just counts). Added "Dashboard" to `AdminNav` above Products.
  - **Failed activation attempts**: new `activation_attempts` table — every time `submitActivation` rejects a phone number that doesn't match an order, it now logs the attempt (name/phone/email entered) here instead of just returning an error and forgetting it happened. Admin Activations page now shows a "N failed attempts" badge plus a full table of who tried with which wrong number, alongside the existing successful-activations table.
  - **Product image gallery, Apple-style**: new `product_images` child table (product_id, image_url, sort_order) — `products.image_url` stays as the single "cover" image used on Shop listing cards/OG previews, while the detail page now combines cover + gallery into one ordered image set. New `src/components/shop/ImageGallery.tsx` (large hero image + thumbnail rail, click a thumbnail to switch, accent-bordered active state) replaces the old single static image block. Admin: new `GalleryManager.tsx` inside the product edit modal — upload multiple images at once, delete individual ones (with hover-reveal × button), storage cleanup on delete (removes the file from the `product-images` bucket, not just the DB row). Gallery management only appears once a product has been saved once (needs a real id to attach images to).
  - **Sticky Buy Now bar**: `src/components/shop/StickyBuyBar.tsx` — a slim fixed bottom bar (title + price + Buy now button) that fades in via `IntersectionObserver` once the user scrolls past the main buy button, on both mobile and desktop. Clicking it smooth-scrolls back up to the real CTA rather than duplicating the checkout form in two places.
    - **Caught and fixed a real bug during testing**: the observer was originally watching the *entire* product-info block (title + description + price + checkout form), which is tall enough that on a short product page it never fully left the viewport — the sticky bar would never appear at all. Fixed by adding a dedicated 0-height sentinel (`#buy-sentinel`) positioned right at the CTA, independent of the checkout form's expand/collapse height changes, and confirmed with Playwright that it now toggles correctly at the right scroll position on both viewport sizes.
  - **Graceful degradation for not-yet-migrated tables**: `getProductImages`/`getAllProductImages`/`getActivationAttempts` all fail soft (return `[]`) instead of throwing, so a missing `product_images`/`activation_attempts` table (before the SQL below is run) degrades to "no gallery images yet" / "0 failed attempts" rather than 500-ing the entire product page or Activations tab — learned from the order-confirmation bug earlier not to let an additive feature become a single point of failure for core pages.
  - New `supabase/004_activation_attempts_and_product_images.sql` — **not yet run**, needed before failed-attempt logging and the product gallery actually work with real data. Both new tables have RLS enabled with the same anon-insert/authenticated-manage pattern as the rest of the schema.
  - Verified: full type-check clean, production build succeeds with the expected static/dynamic route split, all public routes smoke-tested, sticky bar and gallery visually confirmed via Playwright screenshots on desktop and mobile.
  - User ran the SQL. Hit a follow-up bug uploading gallery images: Next.js caps Server Action request bodies at 1MB by default, way too small for real photo uploads. Fixed with `experimental.serverActions.bodySizeLimit: '20mb'` in `next.config.ts` (requires a dev-server restart, doesn't hot-reload).
- **Upgraded the product gallery to a carousel + zoom lightbox**, per follow-up request. `ImageGallery.tsx`: added hover-reveal prev/next arrow buttons and touch-swipe on the main image (in addition to the existing thumbnail-click), plus a corner zoom icon. New `ImageLightbox.tsx`: full-screen overlay with scroll-wheel/± button zoom (1x–4x), double-click to toggle zoom, drag-to-pan when zoomed (pointer events), prev/next arrows and arrow-key navigation, image counter, Esc/backdrop-click/× to close, body-scroll lock while open. Verified end-to-end against a real product with 5 uploaded images (cover + 4 gallery) via Playwright: hover arrows, lightbox open, zoom in, in-lightbox next, Escape close — all confirmed working, screenshots checked visually.
- **Added a multi-item cart, real delivery zones/fees, admin delete buttons, and a real order ID display** — four requests in one batch:
  - **Admin delete buttons**: Orders, Activations (both the successful table and the failed-attempts table), and Messages each got a Delete action. `orders`/`activations`/`messages`/`activation_attempts` never had DELETE RLS policies before (only insert/update existed), so all four were added in the new SQL migration below — the buttons render now but do nothing against live data until that migration runs. Activations and Messages pages were split into thin Server Component `page.tsx` fetchers + new client `ActivationsView.tsx`/`MessagesTable.tsx` components (`useTransition` + `confirm()` before deleting), matching the pattern already used for Orders.
  - **Real order ID on the admin Orders tab**: previously showed nothing identifying the order. Added an "Order ID" column showing the same short id (`id.slice(-8).toUpperCase()`) customers see on their confirmation/tracking pages, so admin and customer are always looking at the same reference number.
  - **Shopping cart**: new client-side-only cart (`src/lib/cart/CartContext.tsx`, React Context + `localStorage`, no server-side cart table — consistent with the site having no customer accounts). Minimalist bag-icon cart link added to the header, positioned before "Shop now", with an accent count badge. Product page now has two separate actions: "Add to cart" (adds to the persistent cart, shows an "Added ✓" confirmation) and "Buy now · Cash on Delivery" (unchanged direct single-item checkout, unaffected by the cart). New `/cart` page (`CartView.tsx` + `placeCartOrder` action): per-item quantity/remove controls, delivery zone selector, and one checkout form that re-derives authoritative prices server-side from the product table (never trusts cart totals from the client) before inserting the order.
  - **Delivery zones**: both the direct Buy Now checkout and the cart checkout now require picking "Inside Dhaka" (৳70) or "Outside Dhaka" (৳130), defined once in `src/lib/delivery.ts` and imported everywhere so the fee only ever lives in one place. Order confirmation and order tracking pages now show a Subtotal / Delivery / Total breakdown instead of a flat total, using the same labels.
  - **New multi-item order schema, additive/non-destructive**: new `supabase/005_cart_checkout_and_admin_deletes.sql` — **not yet run**. Adds `orders.delivery_zone`/`delivery_fee`/`subtotal`/`total`; relaxes the legacy `product_title`/`product_price`/`quantity` columns to nullable (existing rows untouched); adds a new `order_items` child table for multi-item orders; adds the four missing DELETE policies above; rewrites `get_order_confirmation`/`track_order` to return an `items` array (via `json_agg` over `order_items`) with a `coalesce()` fallback to the legacy flat columns so orders placed before this migration still render correctly. `getAllOrderItems()` (admin) and both RPCs fail soft to the legacy display when `order_items` is empty, so nothing breaks before the migration is run — only new order placement needs it.
  - Verified: `npx tsc --noEmit` clean. Playwright smoke test on `/shop/g1`: Add-to-cart badge updates and persists across a full reload (localStorage confirmed working), Buy Now reveals the delivery zone selector with a live-updating total (99 + 70 delivery = 169, correct), `/cart` page renders the item, delivery selector, and subtotal/delivery/total breakdown correctly. Zero console/page errors throughout.
  - Still needed: user to run `supabase/005_cart_checkout_and_admin_deletes.sql`, after which cart checkout, admin order-item display, and all new delete buttons will work against live data.
- **Switched currency to BDT, added a printable delivery slip, and a new admin Contacts tab** — three follow-up requests (business operates in Bangladesh):
  - **Currency → BDT**: `formatPrice()` (`src/lib/utils.ts`) is the single place every price renders through across the whole site (grepped to confirm — no other file hardcodes a `$`), so switching its symbol from `$` to `৳` updated every price display (Shop listing/detail, cart, checkout, order confirmation, tracking, admin Orders/Contacts) in one place. No underlying numeric values changed — this is a display-format change only, not a currency conversion.
  - **Printable delivery slip**: Orders admin table got a "Print" action per row, opening `/admin/print/orders/[id]` in a new tab — a dedicated, deliberately plain white/black page (outside the normal dark admin layout, with its own inline `getUser()` auth check rather than inheriting the `(protected)` layout) showing exactly what a courier needs: customer name/phone/address, delivery zone, itemized products, and the subtotal/delivery/total breakdown. A "Print" button (hidden via `print:hidden` when actually printing) triggers `window.print()`; from there the user can print to paper or choose "Save as PDF" in the print dialog. Added `getOrderById`/`getOrderItemsForOrder` to `src/lib/data/orders.ts` to back it.
  - **New admin Contacts tab** (`/admin/contacts`): combines every customer who has either placed an order or sent a message into one place — a "From orders" table (name, phone, address, delivery zone, total, date) and a "From messages" table (name, email, subject, date), reusing the existing `getOrders()`/`getMessages()` data functions directly (no new tables or schema needed). Added a range filter — Last 7 days / Last 30 days / Last 365 days / All time (`src/lib/contacts.ts`) — filtering both tables client-side by `created_at`, plus a "Download PDF" button that generates a real, immediately-downloadable `.pdf` file (via the newly added `jspdf` + `jspdf-autotable` packages) containing both tables for whichever range is currently selected — a genuine one-click export rather than relying on the browser's print-to-PDF dialog, since this data (unlike the delivery slip) is meant to be archived/shared, not printed on paper.
  - Verified: `npx tsc --noEmit` clean. Playwright: Shop listing and product detail pages confirmed showing `৳` with zero remaining `$` anywhere on either page; `/admin/orders`, `/admin/contacts`, and `/admin/print/orders/[id]` all correctly redirect an unauthenticated visitor to `/admin/login`, confirming the auth guard covers the two new routes (including the print page's standalone check, since it deliberately sits outside the shared `(protected)` layout). Visual verification of the Print layout and the Contacts tab itself (both require an authenticated admin session) is still pending the user's own check.
- **Fixed a real bug in the Contacts PDF export, and added a fixed/percentage coupon system** — two follow-up requests:
  - **Bug fix — garbled prices in the downloaded PDF**: the user reported prices rendering as corrupted characters (e.g. `&ó&9&9&.&0&`) in the Contacts "Download PDF" file. Root cause: jsPDF's built-in fonts (Helvetica/Times/Courier) only support the WinAnsi/Latin-1 character set, and `formatPrice()`'s `৳` glyph (added in the previous BDT change) falls well outside that range, so jsPDF corrupted the whole cell's text, not just the symbol. Fixed with a PDF-only `pdfPrice()` helper in `ContactsView.tsx` that formats amounts with a plain ASCII `Tk` prefix instead — the on-screen HTML table still shows `৳` correctly, since browsers render Unicode fine; only jsPDF's fixed font set can't.
  - **New coupon system**: minimal fixed-amount (৳) or percentage (%) discount codes, usable on both the direct "Buy now" checkout and the cart checkout.
    - New `supabase/006_coupons.sql` — **not yet run**. Adds a `coupons` table (code, discount_type, discount_value, is_active) with no anon SELECT policy (codes are validated only through a new `validate_coupon(code)` SECURITY DEFINER RPC, so the full coupon list can't be scraped via the public REST API — same pattern as order tracking). Adds `orders.coupon_code`/`discount_amount` columns, and rewrites `get_order_confirmation`/`track_order` to also return those two fields.
    - New `src/lib/coupons.ts` (`computeDiscount()` — shared fixed/percent math, clamped so a discount can never exceed the subtotal or go negative) and `src/lib/actions/coupon.ts` (`checkCoupon()` — a shared Server Action both checkout forms call for a live "Apply" preview).
    - `OrderForm.tsx` and `CartView.tsx` both gained a coupon code field with an "Apply" button; on success it shows "Coupon SAVE10 applied" with a Remove option and the total recalculates live. `placeOrder`/`placeCartOrder` **re-validate the code server-side via the same RPC before billing** — never trust the client's displayed discount — and store `coupon_code`/`discount_amount` on the order. Order confirmation, order tracking, and the printable delivery slip all show a "Discount (CODE)" line when one was applied.
    - New admin **Coupons** tab (`/admin/coupons`): add/edit modal (code, Fixed/Percent type, value, active checkbox — mirrors the existing Products add/edit pattern), a one-click Active/Inactive toggle per row, and delete. `getCoupons()` fails soft (returns `[]`) if the table isn't migrated yet, consistent with every other additive feature in this project.
  - Verified: `npx tsc --noEmit` clean. Playwright: applying a coupon code on both the product page and the cart page (against the live, not-yet-migrated database) correctly shows "That coupon code is not valid." with zero console errors — confirming the whole flow fails soft rather than crashing before `006_coupons.sql` is run; `/admin/coupons` correctly redirects an unauthenticated visitor to `/admin/login`. Screenshots confirm the coupon field, error state, and totals breakdown render correctly on both checkout surfaces.
  - Still needed: user to run `supabase/006_coupons.sql`, after which coupon codes can be created in the new admin Coupons tab and applied at checkout. (`005_cart_checkout_and_admin_deletes.sql` from the previous entry is still pending too, if not already run.)
- **Deployed to Vercel; found and fixed a real production bug — all order placement was silently broken.** After the user ran migrations 005 and 006 and deployed to Vercel, every checkout (Buy Now and cart) failed with a generic "Something went wrong placing your order" error.
  - **Root cause**: the original `orders` INSERT RLS policy (from `schema.sql`, written before the cart feature existed) requires the order's `product_id` column to reference an active product: `with check (exists (select 1 from products p where p.id = product_id and p.is_active))`. Migration 005 moved line items out of `orders` and into the new `order_items` table, so `placeOrder`/`placeCartOrder` stopped setting `product_id` on the `orders` row at all — meaning every new order has `product_id = null`, which makes that policy's check always evaluate false and Postgres silently rejects the insert. This was a genuine oversight in migration 005 (the insert policy should have been updated alongside the schema change) and went undetected because no test in this session had actually submitted a real order against a live database with the new schema — only form-filling and coupon-validation were exercised end-to-end before this.
  - **Fix**: new `supabase/007_fix_orders_insert_policy.sql` — **not yet run**. Drops and recreates the policy as `with check (true)`, since product/price validation already happens server-side in `placeOrder`/`placeCartOrder` before the insert runs (the RLS check was redundant defense-in-depth, not the actual guard).
  - Walked the user through their first Vercel deployment: importing the GitHub repo, setting `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`NEXT_PUBLIC_SITE_URL` as environment variables, and flagged Vercel's serverless function request-body ceiling (historically ~4.5MB) as a thing to test against the `bodySizeLimit: '20mb'` Server Action setting once real phone-camera gallery photos get uploaded in production.
  - Still needed: user to run `supabase/007_fix_orders_insert_policy.sql` — order placement will not work at all until this runs.